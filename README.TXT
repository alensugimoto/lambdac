This implements an interpreter for lambda calculus
To run the interpreter, simply call the main method of the Main
class and then you can type your input into the tab

The model package contains everything needed to interpret the 
input: lexer, ast and parser

The gui package contains the parts that handle the graphical user 
interface

The tui has nothing in it yet